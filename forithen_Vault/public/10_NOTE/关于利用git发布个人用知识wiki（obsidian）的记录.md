- 类型：个人知识发布 / 静态站点
- 技术栈：Obsidian / GitHub / GitHub Pages / MkDocs
- 目标：将本地知识以低成本方式长期公开

发布流程
Obsidian → Git → CI → GitHub Pages

主要的知识点
1.  [[#GitHub中Mkdoc的应用方式]]
2. [[#MkDocs 的 Mechanism]]
3. [[关于Action]]
4. [[关于Page]]

## GitHub中Mkdoc的应用方式
MkDocs 在 GitHub 里的核心定位
MkDocs = 把 GitHub 上的 Markdown 仓库，自动变成一个可访问的静态文档网站的引擎。
**核心思想：**

- GitHub 管理源码（Markdown）
    
- MkDocs 负责“渲染”
    
- GitHub Actions 负责“自动化”
    
- GitHub Pages 负责“对外发布”
git push
↓
GitHub Actions 自动运行
↓
MkDocs build
↓
生成静态 HTML
↓
GitHub Pages 对外提供访问

**特点**

- 仓库本身就是文档
    
- README + docs 构成完整站点
    

**适合**

- 开源项目文档
    
- 内部技术规范
    
- 学习笔记公开

插件生态 Mkdoc
常见插件分类：
#### 🔸 链接增强

- `roamlinks`：支持 `[[Wiki Link]]`
    
- `awesome-pages`：自动生成导航

#### 🔸 内容增强
- `macros`：变量/模板
    
- `tags`：标签系统

#### 🔸 体验增强
- `search`：全文搜索
    
- `minify`：压缩 HTML

### MkDocs vs Sphinx vs Docusaurus（工程向对比）
果你的核心是「Markdown 文档 + GitHub + 自动发布」  
→ MkDocs 是性价比和复杂度最优解。
MkDocs  文档站生成器（极简、工程友好）
Sphinx    文档编译系统（学术 / 语言文档）
Docusaurus    文档型 Web Site（偏前端）

#### MkDocs

- 原生 Markdown
    
- 结构 = 文件结构
    
- 认知成本极低
    
- 非程序员也能写
    

👉 **Obsidian / Notion 迁移最顺**

#### Sphinx

- reStructuredText（rst）为主
    
- Markdown 是“后加的”
    
- 语法复杂、严格
    

👉 **适合写“说明书”，不适合写“知识体系”**

#### Docusaurus

- Markdown + React
    
- 有 Front Matter（YAML）
    
- 文档是“页面”，不是“文件”
    

👉 **写作体验偏 Web，而不是笔记**

![[Pasted image 20260205001958.png]]

MkDocs 是“文档工程化”的最小解，  
Sphinx 是“说明书系统”，  
Docusaurus 是“文档型网站框架”。

## MkDocs 的 Mechanism
MkDocs 的本质是：  
用一个配置文件，定义“如何把一堆 Markdown 文件，确定性地编译成一套静态 HTML 文件”。
不是 CMS，也不是框架，而是**编译器 + 规则引擎**。

mkdocs.yml（控制中枢）
`mkdocs.yml` 决定三件事：

1. **扫哪里**（docs_dir）
    
2. **怎么组织页面**（nav）
    
3. **怎么渲染**（theme / plugins）
    

它的地位相当于：

- 编译参数
    
- 链接规则
    
- UI 设定
### Theme（渲染引擎）

MkDocs 不直接生成 HTML，而是：

> Markdown → AST → Theme 模板 → HTML

Theme 决定：

- 页面结构
    
- 侧边栏
    
- 搜索框
    
- 手机适配
    

👉 `mkdocs-material` 之所以强，是因为它的模板和 JS 生态成熟。

### site/（输出域）

- `mkdocs build` 的结果
    
- **纯静态文件**
    
- 可直接丢给：
    
    - GitHub Pages
        
    - S3
        
    - Nginx
        

👉 **Pages 只管托管，不管你怎么生成**

### index.md 的特殊地位

规则非常简单但致命：

> **docs_dir/index.md = 网站根路径 `/`**

如果它：

- 不存在
    
- 不在 docs_dir 根
    

👉 你就会看到 **404 / 空页面**

### Obsidian 的 `[[Wiki Link]]` 为什么不生效？

因为：

> **MkDocs 默认不认识 `[[xxx]]` 语法**

解决方案只有两个：

- ✅ 用插件（如 roamlinks）
    
- ❌ 不装插件（当普通文本）
    

👉 插件是在 **Markdown → HTML 之前** 做“语法翻译”。

实际的代码
site_name: B612
site_url: https://costbinne.github.io/obsidianVault/

theme:
  name: material

docs_dir: forithen_Vault/public
site_dir: site

nav:
  - Home: index.md

plugins:
  - search
  - roamlinks
